{
  "devDependencies": {
    "eslint": "^8.35.0",
    "prettier": "^2.8.4"
  },
  "scripts": {
    "lint": "eslint .",
    "format": "prettier --write ."
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm run lint && npm run format && git add ."
    }
  }
}
